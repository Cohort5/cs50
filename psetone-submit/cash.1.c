#include <cs50.h>
#include <math.h>
#include <stdio.h>

int main (void)
{

int c; // change

     c = get_int("How much change do you have? ");

float f;

do
{
    f = get_float("Change: ");
}
while (f > 0 c ! 0);  // invalid

    for (i = 0; i == c < f; i++)  //

}

printf ('I have %i coins(s)\n', n); // final number of coins