//writting functions from our solution - example code

int get Item Index( int item, int rMin)
{
    return item - rMin;
}
int encrypt(int Item, Key)
{
    return iItem + key;
}

int modifier = imin + 1;
int returnToRange = rMin

int itemIndex = getItemIndex(item, rMin);
printf("itemIndex should be 17 it is %i\n", itemIndex)

int encryptedItem = encrypt(itemIndex, key)
// int modifier = getMod(iMin)
// int returnToRange = rMin
int convertedIndex = encryptedItem % modifier

convertedIndex + returnToRange

// decrypt(getItemIndex(item, rMin) key) % (imin +1) + rmin